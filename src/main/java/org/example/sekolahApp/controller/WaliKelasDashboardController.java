package org.example.sekolahApp.controller;

import javafx.fxml.FXML;

public class WaliKelasDashboardController {
    // ...
    @FXML
    private void handleCetakRapor() {
        // 1. Tampilkan daftar siswa di kelas perwaliannya.
        // 2. Setelah siswa dipilih, buka window baru (Stage baru).
        // 3. Di window baru, load FXML untuk rapor.
        // 4. Controller FXML rapor akan mengambil data nilai siswa tsb dan menampilkannya.
        // 5. Sediakan tombol Print (bisa menggunakan PrinterJob API JavaFX).
    }
}